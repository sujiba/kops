---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./secrets.sops.yaml

configMapGenerator:
  - name: configarr-configmap
    files:
      - config/config.yml

generatorOptions:
  disableNameSuffixHash: true

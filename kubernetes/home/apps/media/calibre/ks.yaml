---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: calibre
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: calibre
  components:
    - ../../../../components/kopiur/backup
  interval: 1h
  path: ./kubernetes/home/apps/media/calibre/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: volsync-kopia-secret
        kind: Secret
    substitute:
      APP: calibre
      VOLSYNC_CLAIM: calibre-data
      VOLSYNC_CAPACITY: 10Gi
      VOLSYNC_GID: "1000"
      VOLSYNC_UID: "1000"
      KOPIUR_CAPACITY: 1Gi
      KOPIUR_PUID: "1000"
      KOPIUR_PGID: "1000"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: false

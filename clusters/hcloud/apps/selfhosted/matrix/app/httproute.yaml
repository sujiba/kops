---
# HTTP Route for Synapse (Matrix homeserver)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-synapse
  namespace: selfhosted
spec:
  hostnames: ["matrix.${EXTERNAL_DOMAIN}"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-synapse
          namespace: selfhosted
          port: 8008

---
# HTTP Route for Matrix Authentication Service (MAS)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-auth-service
  namespace: selfhosted
spec:
  hostnames: ["mas.${EXTERNAL_DOMAIN}"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-matrix-authentication-service
          namespace: selfhosted
          port: 8080

---
# HTTP Route for Matrix RTC (Real-Time Communication)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-rtc
  namespace: selfhosted
spec:
  hostnames: ["rtc.${EXTERNAL_DOMAIN}"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /sfu/get
      backendRefs:
        - name: matrix-matrix-rtc-authorisation-service
          namespace: selfhosted
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-matrix-rtc-sfu
          namespace: selfhosted
          port: 7880

---
# HTTP Route for Element Admin
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-admin
  namespace: selfhosted
spec:
  hostnames: ["madmin.${EXTERNAL_DOMAIN}"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-element-admin
          namespace: selfhosted
          port: 8080

---
# HTTP Route for Element Web
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-element
  namespace: selfhosted
spec:
  hostnames: ["chat.${EXTERNAL_DOMAIN}"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: matrix-element-web
          namespace: selfhosted
          port: 80

---
# HTTP Route for Well-Known Delegation
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-well-known
  namespace: selfhosted
spec:
  hostnames: ["matrix.${EXTERNAL_DOMAIN}"]
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /.well-known/
      backendRefs:
        - name: matrix-well-known
          namespace: selfhosted
          port: 8010